# Rendered output and the working frame directory. The source is the template;
# the video is a build artefact and is regenerated by `render.mjs`.
node_modules/
.frames/
*.mp4
still-*.png

# Built from ad.template.html by build-ad.mjs, with the fonts inlined.
scenes/ad.html
