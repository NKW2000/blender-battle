{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @bb/shared build && pnpm --filter @bb/api build",
  "framework": null,
  "//outputDirectory": "This project is only serverless functions, but Vercel still looks for a static output directory once the build finishes and fails the deploy without one. `public/` exists for that reason and is kept empty — see the note inside it.",
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "crons": [{ "path": "/api/v1/maintenance/sweep", "schedule": "0 3 * * *" }]
}
